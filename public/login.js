const form=document.querySelector('#loginForm'),email=document.querySelector('#email'),errorBox=document.querySelector('#loginError');
(async()=>{try{const d=await fetch('/api/auth/me').then(r=>r.json());if(d.user)location.replace('/admin');}catch{}})();
form.addEventListener('submit',async e=>{e.preventDefault();errorBox.classList.add('hidden');const value=email.value.trim();if(!value)return;const btn=form.querySelector('button');btn.disabled=true;try{const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:value})});const d=await r.json();if(!r.ok)throw new Error(d.error||'No fue posible ingresar.');location.replace('/admin');}catch(err){errorBox.textContent=err.message;errorBox.classList.remove('hidden');btn.disabled=false;}});
